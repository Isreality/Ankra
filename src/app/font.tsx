import { Raleway } from "next/font/google";
 
// export const plus = Plus_Jakarta_Sans({ subsets: ['latin'] });

export const raleway = Raleway({
  variable: "--font-raleway",
  subsets: ["latin"],
});